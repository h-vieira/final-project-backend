import colors from 'colors';
import EdiblePinsModel from '../models/ediblePins.js';

const localEdibles = async (req, res) => {
    try {
        
    } catch (error) {
        res.status(500).json(error.message);
    }
};

export default localEdibles;